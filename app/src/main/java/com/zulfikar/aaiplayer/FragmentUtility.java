package com.zulfikar.aaiplayer;

import android.view.MotionEvent;
import android.view.View;

public interface FragmentUtility  {
    void hideView(View titleBar, View bottomNavBar);
    void showView(View titleBar, View bottomNavBar);
}
